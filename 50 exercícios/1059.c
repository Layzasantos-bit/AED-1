#include <stdio.h>

int main() {
    // Percorrer todos os números pares de 2 a 100
    for (int i = 2; i <= 100; i += 2) {
        printf("%d\n", i);  // Imprime cada número par
    }

    return 0;
}
