#include <stdio.h>
 
int main(){
  double   n=3.14159,area,raio, mult;
    
     scanf("%lf", &raio);
      mult=raio*raio;
      area=(n*mult);
     printf("A=%.4lf\n", area );
 
  return 0;
}

